# DataBaseValidator
Python-based tool to validate inventory database normalization across countries. It reads a master mapping dictionary, the original data, and each country’s OUTPUT file, then recomputes the transformation and compares parameters, models and totals. It generates Excel reports with detailed errors and heatmaps, plus a simple UI for file upload.
